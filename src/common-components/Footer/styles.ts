import styled from "styled-components";

export const Container = styled.div`
  background: var(--primary);

  div{
    max-width:1280px;
    padding: 2rem 1rem;
    margin: 0 auto;
    color: var(--white);
  }
`